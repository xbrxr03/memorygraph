# Development Scenarios

This directory is reserved for procedurally varied development fixtures that should not be copied into held-out evaluation.

The current repository ships only the compact public seed set in `../public/`.
