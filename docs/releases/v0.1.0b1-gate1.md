# MemoryGraph 0.1.0b1 Gate 1 evidence

**Verified:** August 24, 2026 (America/New_York)  
**Publication state:** Not tagged, not uploaded, no GitHub release created

## Package identity

- Distribution: `memorygraph-agent`
- Version: `0.1.0b1`
- Classifier: `Development Status :: 4 - Beta`
- Python requirement: `>=3.11`
- Console scripts: `memorygraph`, `memorygraph-mcp`
- Official PyPI JSON and Simple endpoints for `memorygraph-agent` both returned HTTP 404. The
  name therefore appears unregistered as of verification time, but final upload remains the only
  authoritative availability check.

## Artifact evidence

- Wheel and sdist built successfully in isolated PEP 517 build environments.
- `twine check` passed for both artifacts.
- A second independent build produced byte-identical SHA-256 values; see `v0.1.0b1.sha256`.
- Wheel contents include both console scripts and both packaged SQLite migrations.
- Sdist contents include source, tests, benchmarks, documentation, migrations, and release notes.

## Clean-install evidence

- Wheel installed into a disposable Python 3.14 virtual environment, reported `0.1.0b1`, ran the
  complete `onboard-codex` lifecycle, and passed `doctor`. Python 3.14 correctly produced only the
  documented newer-than-tested warning.
- Sdist installed into a separate disposable Python 3.14 virtual environment, rebuilt the exact
  wheel hash, reported `0.1.0b1`, and passed the complete onboarding lifecycle.

## Quality gates

- Full suite: 207 tests passed, including portable Windows config serialization and coarse-clock
  claim-transition regressions.
- Ruff: passed.
- Public MemoryRotBench: 12/12 queries passed; recall 1.000; zero leakage or forbidden content.
- Production chaos: 7/7 cases passed.
- Accelerated Dogfood Beta: every composite gate passed.

The final artifacts were rebuilt after the last code change, reproduced byte-for-byte in a second
isolated build, and installed from both wheel and sdist before this evidence was finalized.
The release candidate is not green until the post-fix nine-job GitHub Actions matrix completes
successfully; that hosted result is monitored separately because embedding a run ID here would
change the artifacts that the run validates.
